# 5.25 卡顿问题修改记录

## 执行说明

按最新要求，已放宽原先“每步完整录制性能 + 像素 0 差异”的严格验收，优先完成低风险性能优化并保持 UI 结构、样式类名和动画配置不变。未伪造缺失的 DevTools Profiler/FPS 数据。

## 提交记录

| 顺序 | Commit | 内容 | 主要文件 |
|---|---|---|---|
| S2 | `142ab88` | ECharts option 引用稳定化 | `apps/editor/features/energy-insights/components/energy-twin-dashboard.tsx` |
| S4 | `31d6b08` | ECharts 按需注册 | `apps/editor/lib/echarts-bundle.ts`, `energy-twin-dashboard.tsx` |
| S6 | `0ac915f` | 给关键面板增加 CSS containment | `apps/editor/app/globals.css`, `energy-twin-dashboard.tsx` |
| S1 | `76cb92c` | 三处时钟合并到共享 TimeStore | `apps/editor/features/host-shell/lib/time-store.ts`, `host-workbench.tsx`, `smart-operations-workspace.tsx` |
| S7 | `f423f42` | 替换 `JSON.stringify(nodes)` 为浅引用变更检测 | `apps/editor/features/host-shell/components/host-workbench.tsx` |
| S5 | `8c8b3bf` | analytics 后台高频更新降优先级 | `data-analysis-workspace.tsx` |
| S1 fix | `affb571` | 修复 `.ts` 文件中 JSX 导致的编译错误 | `time-store.ts` |
| S3 | `b8d2a1e` | 三个大屏热点叶子组件 `memo` 化 | analytics / energy / operations 三个大屏组件 |
| S7 | `34bea66` | analytics 派生数组、排序、投影计算缓存 | `data-analysis-workspace.tsx` |

## 指标记录

已有基线文件：`docs/perf-baseline.md`。  
已有一次 S2 采样文件：`docs/perf-after-S2.md`。  
后续步骤未逐步重录，原因是用户明确要求放宽验收并加快完成。

| 页面 | 阶段 | FPS 5s 平均 | Long Task | React commit | 单 commit 平均 | JS Heap |
|---|---|---:|---:|---:|---:|---:|
| 数据分析大屏 | baseline | 0.00 | 29 | 47 | 0.000 ms | 88.62 MB |
| 能耗查询大屏 | baseline | 7.50 | 25 | 345 | 0.000 ms | 287.63 MB |
| 智慧运维大屏 | baseline | 0.00 | 26 | 392 | 0.000 ms | 139.46 MB |
| 数据分析大屏 | after-S2 fixed | 11.89 | 154 | 0 | 0.000 ms | 86.44 MB |
| 能耗查询大屏 | after-S2 fixed | 14.38 | 87 | 0 | 0.000 ms | 89.55 MB |
| 智慧运维大屏 | after-S2 fixed | 15.63 | 102 | 0 | 0.000 ms | 106.08 MB |

## 验证结果

已执行 `apps/editor` 级 `tsc --noEmit --project tsconfig.json`。当前仍有既有类型错误，集中在：

- `apps/editor/features/energy-insights/lib/energy-prediction.ts`
- `apps/editor/features/energy-insights/lib/energy-zone-highlight.ts`
- `packages/editor/src/components/modeling-editor-module.tsx`
- `packages/editor/src/components/ui/auto-modeling-importer.tsx`

本次新增/修改的 analytics、operations、energy dashboard 组件未再出现新增 TypeScript 错误；`time-store.ts` 的 JSX 语法错误已修复。

## S8 处理

跳过 S8。三大屏由 `HostWorkbench` 叠加在 `ModelingEditorCoreModule` 之上，能耗查询还提供 3D / 2D / 分屏切换；底层 Viewer Canvas 不是确定不可见状态。为避免破坏相机、灯光、选中态和 WebGPU Canvas 生命周期，本轮不做条件卸载。

## 像素回归结论

未执行严格像素 0 差异回归。原因：页面包含实时数据、时钟、动画帧、WebGPU 渲染和随机采样，高严格度截图在前序记录中已经出现大量非确定性差异。按最新要求，本轮以代码级低风险优化和 TypeScript 检查为主。

# 5.25 卡顿问题修改记录

## 结论

本轮没有完整按原始八项硬约束全部做完。按后续“放宽验收、赶紧完成”的要求，优先完成了低风险、可回退的性能优化，并保留每步独立 commit。

当前最终采样相对 `baseline-fixed`：

- 能耗查询大屏：FPS +31.6%，Long Task -33.3%。
- 智慧运维大屏：FPS +37.1%，Long Task -44.7%。
- 数据分析大屏：FPS +2.4%，JS Heap -3.3%，但 Long Task 增加 8.3%，仍需继续处理顶层 state 风暴。

## 八项完成度检查

| 任务 | 状态 | 说明 | Commit |
|---|---|---|---|
| S1 顶层 setState 风暴下沉 | 部分完成 | 已合并三处时钟为 `TimeStore`；未完成 analytics 顶层大 store 切片、8 个定时器下沉到 leaf selector。 | `76cb92c`, `affb571` |
| S2 ECharts option 稳定化 | 已完成 | 5 处 `ReactECharts` option 已用 `useMemo` 稳定；保留原 `key` 和动画配置。 | `142ab88` |
| S3 拆分超大文件 + React.memo | 部分完成 | 已对 analytics / energy / operations 热点叶子组件加 `memo`；未物理拆分 3000 行 analytics 文件，也未做 store selector props 化。 | `b8d2a1e` |
| S4 ECharts 按需注册 | 已完成 | 新增统一 ECharts bundle，切到 `echarts/core` + `echarts-for-react/lib/core`。 | `31d6b08` |
| S5 useDeferredValue / startTransition | 已完成快速版 | analytics 后台非交互更新使用 `startTransition`，顶层模型展示使用 `useDeferredValue`。 | `8c8b3bf` |
| S6 CSS containment | 部分完成 | 已给能耗页关键卡片/图表框和全局关键区域加 containment；未逐一覆盖三大屏全部 KPI/图表/表格容器。 | `0ac915f` |
| S7 重计算缓存 / Web Worker | 部分完成 | 已替换 `JSON.stringify(nodes)`，并缓存 analytics 热点派生数组/排序/投影；未做 >5ms 测量和 Web Worker 搬迁。 | `f423f42`, `34bea66` |
| S8 R3F Canvas 物理隔离 | 条件跳过 | 三大屏底层 Viewer Canvas 不是确定不可见；能耗页还提供 3D/2D/分屏切换。为避免破坏相机、灯光、选中态和 WebGPU 生命周期，本轮不卸载 Canvas。 | 无 |

## 性能对比

采样脚本：`.tmp_run/perf-capture.mjs`  
基线：`output/perf/baseline-fixed.json`  
最终：`output/perf/after-final.json`  
环境：`apps/editor`，`next dev --port 3002`，1920x1080。

| 页面 | FPS 5s 平均 | FPS 变化 | Long Task 10s | Long Task 变化 | React commit 10s | JS Heap | Heap 变化 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 数据分析大屏 | 18.16 -> 18.60 | +2.4% | 84 -> 91 | -8.3% | 0 -> 0 | 108.50 MB -> 104.94 MB | +3.3% |
| 能耗查询大屏 | 15.12 -> 19.90 | +31.6% | 96 -> 64 | +33.3% | 0 -> 0 | 91.67 MB -> 94.28 MB | -2.8% |
| 智慧运维大屏 | 15.61 -> 21.40 | +37.1% | 103 -> 57 | +44.7% | 0 -> 0 | 100.52 MB -> 88.04 MB | +12.4% |

说明：

- 表中 Long Task / Heap 的正向变化表示减少，负值表示变差。
- React commit 数据为脚本预注入 hook 统计，本轮采样中为 0，不能代表 React DevTools Profiler 的真实 commit 次数。
- 最终采样仍有既有控制台噪声：时间文本导致 hydration mismatch、`THREE.Clock` deprecated。这些不是本轮性能 commit 新增，但会影响采样稳定性。

## 验证结果

已执行：

```powershell
..\..\node_modules\.bin\tsc.exe --noEmit --project tsconfig.json
```

结果：本轮新增/修改的大屏组件未再出现新增 TypeScript 错误；当前仍被既有类型错误阻塞：

- `apps/editor/features/energy-insights/lib/energy-prediction.ts`
- `apps/editor/features/energy-insights/lib/energy-zone-highlight.ts`
- `packages/editor/src/components/modeling-editor-module.tsx`
- `packages/editor/src/components/ui/auto-modeling-importer.tsx`

## Git 信息建议

如果保留当前多 commit 结构，PR 标题建议：

```text
perf(editor): 优化三大屏渲染与图表开销
```

PR 描述建议：

```markdown
## Summary
- 稳定能耗页 ECharts option，并改为 echarts/core 按需注册
- 增加关键面板 CSS containment
- 合并大屏时钟更新到共享 TimeStore
- analytics 后台更新降优先级，并缓存热点派生计算
- 三大屏热点叶子组件 memo 化
- host 编辑态变更检测移除 JSON.stringify(nodes)

## Performance
- 能耗查询大屏：FPS +31.6%，Long Task -33.3%
- 智慧运维大屏：FPS +37.1%，Long Task -44.7%
- 数据分析大屏：FPS +2.4%，Heap -3.3%，Long Task +8.3%（仍需继续 S1 store 下沉）

## Validation
- 已运行 apps/editor tsc；当前失败来自既有类型错误，非本轮大屏组件改动新增
- S8 跳过：Viewer Canvas 不满足确定不可见条件，避免破坏 3D 状态
```

若要 squash 成一个 commit，commit message 建议：

```text
perf(editor): reduce dashboard render and chart overhead
```
